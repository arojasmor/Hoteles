---
title: "Proyecto de Discovery en Hoteles"
author: "Alejandro Rojas Moreno"
date: "23/9/2021"
output:
  html_document:
    theme: cerulean
editor_options:
  chunk_output_type: console
---

## 0 - SET UP
```{r setup, include=FALSE, message=FALSE, warning=FALSE, echo=FALSE, comment=""}
knitr::opts_chunk$set(echo = TRUE)


# se cargan los paquetes
paquetes <- c('tidyverse', 'lubridate', 'knitr', 'kableExtra', 'plotly')

instalados <- paquetes %in% installed.packages()

if(sum(instalados == FALSE) > 0) {
  install.packages(paquetes[!instalados])
}

lapply(paquetes, require, character.only = TRUE)


# se establece el tema general para los gráficos
theme_ts <- theme(panel.border = element_rect(fill = NA, 
                                              colour = "grey10"),
                  panel.background = element_blank(),
                  panel.grid.minor = element_line(colour = "grey85"),
                  panel.grid.major = element_line(colour = "grey85"),
                  panel.grid.major.x = element_line(colour = "grey85"),
                  axis.text = element_text(size = 13, face = "bold"),
                  axis.title = element_text(size = 15, face = "bold"),
                  plot.title = element_text(size = 16, face = "bold"),
                  strip.text = element_text(size = 16, face = "bold"),
                  strip.background = element_rect(colour = "black"),
                  legend.text = element_text(size = 15),
                  legend.title = element_text(size = 16, face = "bold"),
                  legend.background = element_rect(fill = "white"),
                  legend.key = element_rect(fill = "white"))


```

## 1 - PRESENTACIÓN












